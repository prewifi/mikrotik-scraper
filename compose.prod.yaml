
networks:
  prod-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.16.100.0/29

services:
  mikrotik-scraper-prod:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: mikrotik-scraper-prod
    restart: unless-stopped
    volumes:
      - ./inventory:/app/inventory:ro
      - ./config.yaml:/app/config.yaml:ro
      - mikrotik-logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=INFO
    networks:
      - prod-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

volumes:
  mikrotik-logs:
    driver: local
